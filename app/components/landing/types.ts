export type Meal = {
  name: string;
  calories: number;
  protein: string;
  carbs: string;
  fat: string;
  tag: string;
  price: string;
  image: string;
};

export type Plan = {
  title: string;
  detail: string;
  price: string;
};

export type Step = {
  title: string;
  text: string;
};
